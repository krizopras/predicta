########################################################
# Predicta ML v3 - Backend Bağımlılıkları
########################################################

# ==== Core Web Framework (Railway Tarafından Kullanılır) ====
Flask==3.0.3           # Temel web framework
flask-cors==4.0.1      # Cross-Origin Resource Sharing (Çapraz Kaynak Paylaşımı)
gunicorn==22.0.0       # Üretim ortamı için HTTP sunucusu (Railway'de kullanılır)

# ==== HTTP ve Veri Çekme (Parsing) ====
requests==2.32.3       # HTTP istekleri yapmak için
beautifulsoup4==4.12.3 # HTML/XML ayrıştırma (Nesine veri çekimi için)
lxml==5.2.1            # BeautifulSoup için hızlı ayrıştırıcı

# ==== Veri Bilimi ve Makine Öğrenimi (ML) Çekirdeği ====
numpy==1.26.4          # Yüksek performanslı sayısal işlemler
pandas==2.2.2          # Veri manipülasyonu ve analizi
scikit-learn==1.5.2    # Temel ML kütüphanesi (Sürüm uyumluluğu için sabitlendi)
xgboost==2.1.1         # Gradient Boosting (Sürüm uyumluluğu için sabitlendi)
lightgbm==4.5.0        # Light Gradient Boosting (Sürüm uyumluluğu için sabitlendi)
joblib==1.4.2          # Python nesnelerini (modelleri) hızlı kaydetme/yükleme
imbalanced-learn==0.12.3 # Dengesiz veri kümeleri için araçlar
scipy==1.10.1          # Bilimsel ve teknik hesaplama

# ==== Yardımcı ve Sistem Araçları ====
python-dateutil==2.9.0.post0 # Tarih/saat işlemleri için esnek araçlar
psutil==6.0.0          # Sistem süreç bilgilerini alma (isteğe bağlı)
fuzzywuzzy==0.18.0     # Dize benzerliği (string matching)
python-Levenshtein==0.25.1 # Levenshtein mesafesi (Hızlı fuzzywuzzy bağımlılığı)
tqdm==4.66.4           # Döngüler için ilerleme çubuğu

# ==== Loglama ve Zaman ====
colorlog==6.8.2        # Renkli log çıktısı
pytz==2024.1           # Zaman dilimi (timezone) desteği
